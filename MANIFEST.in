# Prune non-runtime frontend dependency trees from release artifacts.
prune spectraagent/webapp/frontend/node_modules

# Exclude common transient/build noise from source distributions.
global-exclude __pycache__
global-exclude *.py[cod]
